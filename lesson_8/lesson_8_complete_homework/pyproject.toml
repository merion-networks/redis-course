[tool.poetry]
package-mode=false

[tool.poetry.dependencies]
python = "^3.12"
redis = "^5.2.1"
uvicorn = "^0.34.0"
fastapi = "^0.115.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
